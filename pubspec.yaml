name: news_app_with_api
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.8.0+1
  pretty_dio_logger: ^1.4.0
  cupertino_icons: ^1.0.8
  bloc: ^8.1.3
  flutter_bloc: ^8.1.6
  firebase_core: ^3.15.1
  firebase_auth: ^5.6.2
  flutter_launcher_icons: ^0.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1 # متوافقة مع SDK 3.8.1

flutter:
  uses-material-design: true

  assets:
  - assets/
flutter_icons:
  android: true
  ios: true
  image_path: "assets/logo.png" # Your icon file path
